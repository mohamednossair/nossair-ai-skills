---
name: spec-init
description: Initialize Spec-Driven Development structure with constitution and templates.
---
Create the SDD project structure for this project.
1. Create `.spec/` directory with subdirectory `tasks/`.
2. Generate `constitution.md` with sections: Tech Stack, Rules, Testing. Pre-fill from `.junie/guidelines.md` if available.
3. Generate `spec.md` template with sections: Goal, Non-Goals, Requirements, Acceptance Criteria.
4. Generate `plan.md` template with sections: Architecture, Data Model, Implementation Steps.
5. Generate `tasks/01-example.md` template showing the atomic task format (Context, Deliverables, Constraints).
6. Keep all templates under 25 lines each — concise, bullet-point format only.
7. Use the spec-kit skill from `.junie/skills/spec-kit/SKILL.md` as reference for structure.
8. Print a summary of created files and next steps.
